import React, { Component } from "react";
import { View } from "react-native";
import styles from "./styles";

class BefrienderFooter extends Component {
    render() {
        return (
            <View style={styles.root} />
        );
    }
}
export default (BefrienderFooter);
import { StyleSheet } from "react-native";

export default StyleSheet.create({
    profile: {

    },
    button: {
        position: 'absolute',
        bottom: 0,
        right: 0,
        overflow: 'hidden',
    },
    anchor_container: {
        alignItems: 'center',
        justifyContent: 'center',
    },
    anchor_button: {
        position: 'absolute',
    },
});